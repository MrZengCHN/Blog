---
trigger: always_on
---

1. This project is a Vue3 project  
2. The CSS framework used is TailwindCSS  
3. The UI framework used is DaisyUI
4. When generating front-end pages, first consider utilizing components from DaisyUI.
5. Minimize direct manipulation by encapsulating components
6. The materials needed for blog preservation are stored in src\assets\blogs\${yyyyMM}\${blogName}
7. The main page of the blog is saved under src\views\blogs\${yyyyMM}\${blogName}.
8. Reusable components within a single blog should be placed under src\components\blogs\${yyyyMM}\${blogName}.